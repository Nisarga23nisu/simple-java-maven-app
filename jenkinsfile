pipeline {
            agent { label 'Jenkins node' }
            tools {
                      maven:'mvn3.9'
            }
        stages {
              stage('source') { 
                  steps { 
                      git url:
            }
        }
     }   
}
